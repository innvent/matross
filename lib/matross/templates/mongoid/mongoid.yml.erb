<%= rails_env %>:
  sessions:
    default:
      database: <%= mongo_database.gsub("-", "_") %>
      hosts:
      <% mongo_hosts.each do |host| -%>
        - <%= host %>
      <% end -%>
      username: <%= mongo_user %>
      password: <%= mongo_passwd %>
